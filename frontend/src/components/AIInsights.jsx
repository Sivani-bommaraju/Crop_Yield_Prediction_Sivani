import {
  Brain,
  CheckCircle2,
  AlertTriangle,
  TrendingUp,
} from "lucide-react";

export default function AIInsights() {
  const insights = [
    {
      icon: <CheckCircle2 className="text-green-600" />,
      title: "Soil Health",
      desc: "Black soil is highly suitable for cotton and rice cultivation.",
    },
    {
      icon: <TrendingUp className="text-blue-600" />,
      title: "Yield Forecast",
      desc: "Estimated yield can improve by 12% with optimized irrigation.",
    },
    {
      icon: <AlertTriangle className="text-yellow-500" />,
      title: "Weather Alert",
      desc: "Light rainfall is expected within the next 48 hours.",
    },
  ];

  return (
    <div className="bg-white rounded-3xl shadow-lg p-8">

      <div className="flex items-center gap-4 mb-8">

        <div className="bg-green-100 p-3 rounded-2xl">
          <Brain className="text-green-700" />
        </div>

        <div>
          <h2 className="text-2xl font-bold">
            AI Insights
          </h2>

          <p className="text-gray-500">
            Recommendations generated by AI
          </p>
        </div>

      </div>

      <div className="space-y-10">

        {insights.map((item, index) => (

          <div
            key={index}
            className="flex gap-4 p-4 rounded-2xl bg-gray-50 hover:bg-green-50 transition"
          >

            {item.icon}

            <div>

              <h3 className="font-semibold">
                {item.title}
              </h3>

              <p className="text-sm text-gray-600 mt-1">
                {item.desc}
              </p>

            </div>

          </div>

        ))}

      </div>

    </div>
  );
}